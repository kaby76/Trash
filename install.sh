version=0.6.0

dotnet tool install -g trconvert --version $version
dotnet tool install -g trfold --version $version
dotnet tool install -g trfoldlit --version $version
dotnet tool install -g trgen --version $version
dotnet tool install -g trgroup --version $version
dotnet tool install -g trjson --version $version
dotnet tool install -g trkleene --version $version
dotnet tool install -g trparse --version $version
dotnet tool install -g trprint --version $version
dotnet tool install -g trrename --version $version
dotnet tool install -g trst --version $version
dotnet tool install -g trstrip --version $version
dotnet tool install -g trtext --version $version
dotnet tool install -g trtokens --version $version
dotnet tool install -g trtree --version $version
dotnet tool install -g trunfold --version $version
dotnet tool install -g trungroup --version $version
dotnet tool install -g trwdog --version $version
dotnet tool install -g trxgrep --version $version
dotnet tool install -g trxml --version $version
dotnet tool install -g trxml2 --version $version

